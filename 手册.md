# Cangjie Compiler Range Analysis 使用手册

本文档面向当前仓库：

```text
/home/gunseller/project/cangjie_compiler
```

内容覆盖三件事：

1. 环境配置；
2. 编译 `cjc` / 使用阶段版 `cjc.stage4`；
3. 如何触发 `input.txt -> output.txt` 的 range analysis 查询流程。

## 1. 仓库与依赖

推荐目录布局如下：

```text
/home/gunseller/project/
  cangjie_compiler/
  cangjie_runtime/
  cangjie_stdx/
  cangjie_tools/
```

`cangjie_compiler` 是当前开发仓库。其余三个仓库是构建和安装所需依赖。

当前 `version1` 构建锁定过的依赖提交如下：

```text
cangjie_runtime  7aa51b2342d1e70ce449e68acd436769467e04d4
cangjie_stdx     becbc92a62fce5a24ccc3a05827eb5879b293581
cangjie_tools    c92878cc3b5487f348ca01df98e6758d8254f075
```

示例拉取方式：

```bash
mkdir -p /home/gunseller/project
cd /home/gunseller/project

git clone -b version1 git@gitlab.com:Gunse11er/operatorparallel.git cangjie_compiler

git clone https://gitcode.com/Cangjie/cangjie_runtime.git
git clone https://gitcode.com/Cangjie/cangjie_stdx.git
git clone https://gitcode.com/Cangjie/cangjie_tools.git

git -C cangjie_runtime checkout 7aa51b2342d1e70ce449e68acd436769467e04d4
git -C cangjie_stdx checkout becbc92a62fce5a24ccc3a05827eb5879b293581
git -C cangjie_tools checkout c92878cc3b5487f348ca01df98e6758d8254f075
```

## 2. 基础环境

在 WSL/Linux 中建议至少准备：

```bash
python3 --version
cmake --version
ninja --version
git --version
```

还需要一个可用的 C/C++ 编译器。当前旧的 `build/build/CMakeCache.txt` 曾记录：

```text
CMAKE_CXX_COMPILER=/usr/bin/clang++
CMAKE_INSTALL_PREFIX=/home/lh/Cangjie/cangjie_compiler/output
```

这说明 `build/build` 里有旧机器或旧路径缓存。如果本机没有 `/usr/bin/clang++`，直接执行：

```bash
ninja -C build/build bin/cjc -j$(nproc)
```

可能会报编译器路径不存在或路径不一致。遇到这种情况，优先清理并重新生成构建目录，不要复用别人机器拷来的 `build/`。

## 3. LLVM 说明

需要区分两个概念：

| 场景 | 使用的 LLVM / 编译器 | 说明 |
| --- | --- | --- |
| 编译 `cangjie_compiler` 源码 | CMake 选中的宿主 C++ 编译器，如 `clang++` 或 `g++` | 这是用来编译 C++ 源码的编译器 |
| 链接进 `cjc` 的 LLVM | 仓颉项目内置/补丁后的 LLVM | 必须与仓颉编译器代码匹配 |

当前仓库中相关路径通常是：

```text
build/build/third_party/llvm
build/codex-build/third_party/llvm
output/third_party/llvm
```

如果编译 `cjc` 时报 LLVM API 不匹配，通常不是普通系统 `clang++` 的问题，而是 CMake 找到的 LLVM 头文件/库版本不对，或者旧 build cache 指向了不匹配的 LLVM。处理方式是清理构建目录并让 `build.py` 使用仓库内匹配的 LLVM 重新配置。

不要随便把 CMake 指到系统自带 LLVM，例如 `/usr/lib/llvm-*`，否则容易出现：

- 编译期 LLVM API 不存在；
- 链接期 undefined reference；
- 头文件版本和库版本不一致。

## 4. 标准可复现编译流程

从仓库根目录执行：

```bash
cd /home/gunseller/project/cangjie_compiler

python3 build.py clean
python3 build.py build -t release -j$(nproc)
python3 build.py install
```

安装后会生成：

```text
/home/gunseller/project/cangjie_compiler/output/
  bin/cjc
  envsetup.sh
  lib/
  modules/
  runtime/
  third_party/llvm/
```

配置运行环境：

```bash
cd /home/gunseller/project/cangjie_compiler
source ./output/envsetup.sh
cjc --version
```

`output/envsetup.sh` 会设置：

```bash
CANGJIE_HOME=/home/gunseller/project/cangjie_compiler/output
PATH=$CANGJIE_HOME/bin:$CANGJIE_HOME/tools/bin:...
LD_LIBRARY_PATH=$CANGJIE_HOME/runtime/lib/...:$CANGJIE_HOME/tools/lib:...
```

如果不想 source，也可以对单条命令显式指定：

```bash
CANGJIE_HOME=/home/gunseller/project/cangjie_compiler/output \
/home/gunseller/project/cangjie_compiler/output/bin/cjc --version
```

## 5. 当前仓库里的不同 `cjc`

| 路径 | 用途 | 建议 |
| --- | --- | --- |
| `output/bin/cjc` | `build.py install` 后安装好的正式编译器 | 推荐给别人拉取后使用 |
| `build/build/bin/cjc` | CMake/Ninja 构建目录里的开发版可执行文件 | 可用于本地快速测试，但依赖 `build/build` 缓存 |
| `build/build/bin/cjc.stage4` | 当前阶段 4 临时链接出来的测试编译器 | 只用于验证阶段 4 改动 |
| `build/codex-build/...` | 当前机器上为了绕开旧 `build/build` 缓存而使用的辅助构建目录 | 不作为交付给别人的标准路径 |

`cjc.stage4` 不是标准安装产物，所以它无法稳定自动推断 `CANGJIE_HOME`。运行它时建议显式设置：

```bash
export CANGJIE_HOME=/home/gunseller/project/cangjie_compiler/output
/home/gunseller/project/cangjie_compiler/build/build/bin/cjc.stage4 --version
```

如果不设置，可能出现：

```text
error: failed to detect cangjie home, reason: invalid cjc home
```

## 6. 只重新编译 `cjc`

如果已经有干净、正确配置过的 `build/build`，可以只构建 `cjc`：

```bash
cd /home/gunseller/project/cangjie_compiler
ninja -C build/build bin/cjc -j$(nproc)
```

这条命令调用的 C++ 编译器不是命令行临时决定的，而是 `build/build/CMakeCache.txt` 中的：

```text
CMAKE_CXX_COMPILER
```

检查方法：

```bash
rg -n 'CMAKE_CXX_COMPILER|CMAKE_INSTALL_PREFIX|LLVM' build/build/CMakeCache.txt
```

如果里面是旧路径，例如 `/usr/bin/clang++` 不存在，或者安装前缀还是 `/home/lh/...`，不要继续硬编，建议：

```bash
python3 build.py clean
python3 build.py build -t release -j$(nproc)
python3 build.py install
```

## 7. `output.txt` 是怎么生成的

当前 range analysis 比赛查询逻辑接在 CHIR range propagation 流程中。

核心代码路径：

```text
src/CHIR/CHIR.cpp
  ToCHIR::RunRangePropagation()
    AnalysisWrapper<RangeAnalysis, RangeDomain> vra(builder)
    vra.RunOnPackage(...)
    RangePropagation::EmitContestOutput(chirPkg, vra)

src/CHIR/Optimization/RangePropagation.cpp
  LoadContestQueries()     读取 input.txt
  EmitContestOutput(...)   遍历 RangeAnalysis 结果
  WriteContestOutput(...)  写 output.txt
```

`RangePropagation.cpp` 里写死了文件名：

```text
input.txt
output.txt
```

它读取的是当前工作目录下的 `input.txt`，写出的也是当前工作目录下的 `output.txt`。

因此必须在测试用例目录里运行编译命令，而不是在仓库根目录随便运行。

## 8. 生成 `output.txt` 的必要条件

必须同时满足：

1. 当前工作目录存在名为 `input.txt` 的文件；
2. `input.txt` 行格式是：

   ```text
   [fileName, line, variableName]
   ```

3. `fileName` 要匹配 debug location 的源文件 basename，例如 `main.cj`；
4. `line` 要匹配变量所在源码行；
5. `variableName` 要匹配源码变量名；
6. 编译命令必须触发 CHIR O2 优化流程，也就是带 `-O2`；
7. 编译必须成功进入 CHIR 优化阶段；
8. `CANGJIE_HOME` 必须有效，或者已经 `source output/envsetup.sh`。

`--dump-chir` 不是生成 `output.txt` 的必要条件。它只用于导出 CHIR 文本，方便排查。

`--output-type=staticlib` 也不是 range 查询本身的必要条件，但测试中推荐使用它，因为它能减少链接可执行文件时的运行时依赖干扰。

推荐命令模板：

```bash
cd /home/gunseller/project/cangjie_compiler/range_analysis_stage4/e2e_expr

export CANGJIE_HOME=/home/gunseller/project/cangjie_compiler/output

../../build/build/bin/cjc.stage4 main.cj \
  -O2 \
  --output-type=staticlib \
  -o stage4_expr_static
```

如果需要 CHIR dump：

```bash
../../build/build/bin/cjc.stage4 main.cj \
  --dump-chir \
  -O2 \
  --output-type=staticlib \
  -o stage4_expr_static
```

命令结束后检查：

```bash
cat output.txt
diff -u expected_output.txt output.txt
```

## 9. 阶段 4 测试示例

阶段 4 用例目录：

```text
/home/gunseller/project/cangjie_compiler/range_analysis_stage4/e2e_expr
```

完整运行：

```bash
cd /home/gunseller/project/cangjie_compiler/range_analysis_stage4/e2e_expr

rm -rf stage4_expr_static stage4_expr_static_CHIR default.cjo output.txt

export CANGJIE_HOME=/home/gunseller/project/cangjie_compiler/output

../../build/build/bin/cjc.stage4 main.cj \
  --dump-chir \
  -O2 \
  --output-type=staticlib \
  -o stage4_expr_static

cat output.txt
diff -u expected_output.txt output.txt
```

期望输出：

```text
[-5, 5:1]
[-5, 5:1]
[-6, 4:1]
[0, 7:1]
[0, 14:1]
[0, 3:1]
[0, 7:1]
[8, 15:1]
[0, 7:1]
false
true
```

## 10. 阶段 2/3 回归测试

阶段 2 分支和 multibranch：

```bash
cd /home/gunseller/project/cangjie_compiler/range_analysis_stage2/e2e_complex

rm -rf stage4_complex_static stage4_complex_static_CHIR default.cjo output.txt

CANGJIE_HOME=/home/gunseller/project/cangjie_compiler/output \
/home/gunseller/project/cangjie_compiler/build/build/bin/cjc.stage4 main.cj \
  --dump-chir \
  -O2 \
  --output-type=staticlib \
  -o stage4_complex_static

diff -u expected_output.txt output.txt
```

阶段 3 递增循环：

```bash
cd /home/gunseller/project/cangjie_compiler/range_analysis_stage3/e2e_loop

rm -rf stage4_loop_static stage4_loop_static_CHIR default.cjo output.txt

CANGJIE_HOME=/home/gunseller/project/cangjie_compiler/output \
/home/gunseller/project/cangjie_compiler/build/build/bin/cjc.stage4 main.cj \
  --dump-chir \
  -O2 \
  --output-type=staticlib \
  -o stage4_loop_static

diff -u expected_output.txt output.txt
```

阶段 3 递减循环：

```bash
cd /home/gunseller/project/cangjie_compiler/range_analysis_stage3/e2e_loop_descending

rm -rf stage4_desc_static stage4_desc_static_CHIR default.cjo output.txt

CANGJIE_HOME=/home/gunseller/project/cangjie_compiler/output \
/home/gunseller/project/cangjie_compiler/build/build/bin/cjc.stage4 main.cj \
  --dump-chir \
  -O2 \
  --output-type=staticlib \
  -o stage4_desc_static

diff -u expected_output.txt output.txt
```

## 11. `input.txt` 编写规则

每行一个查询：

```text
[main.cj, 4, bounded]
[main.cj, 5, negBounded]
```

注意：

- `main.cj` 是源码文件 basename，不需要绝对路径；
- 行号是源码里的 1-based 行号；
- 变量名来自源码变量名；
- 查询顺序就是 `output.txt` 的输出顺序；
- 空行或格式错误行不要写，否则会得到 fallback 结果，影响比对；
- 对 `let x = ...` 这种局部变量，通常依赖 CHIR `Debug` 表达式定位；
- 如果变量被优化掉、行号不对或变量名不对，可能输出类型全域 fallback。

## 12. 输出格式

整数：

```text
4
[-5, 5:1]
[0, 18446744073709551615:1]
```

布尔：

```text
true
false
false, true
```

当结果无法精确定位或当前 transfer 不支持时，会输出 sound fallback：

- Bool fallback：`false, true`
- signed integer fallback：对应有符号类型全域
- unsigned integer fallback：对应无符号类型全域
- 类型未知 fallback：当前实现使用 Int64 全域

## 13. 常见问题

### 13.1 `failed to detect cangjie home`

原因：没有设置 `CANGJIE_HOME`，或者运行的是 `cjc.stage4` 这类临时可执行文件，自动推断失败。

解决：

```bash
export CANGJIE_HOME=/home/gunseller/project/cangjie_compiler/output
```

或使用安装版：

```bash
cd /home/gunseller/project/cangjie_compiler
source ./output/envsetup.sh
output/bin/cjc --version
```

### 13.2 没有生成 `output.txt`

按顺序检查：

```bash
pwd
ls input.txt
echo "$CANGJIE_HOME"
```

确认命令里有 `-O2`：

```bash
../../build/build/bin/cjc.stage4 main.cj -O2 --output-type=staticlib -o test_static
```

如果没有 `-O2`，`RunRangePropagation()` 会直接返回，不会调用 `EmitContestOutput()`，因此不会生成 `output.txt`。

### 13.3 `output.txt` 全是全域

常见原因：

- `input.txt` 行号不对；
- 变量名不对；
- 源文件名不匹配；
- 查询变量没有对应 debug 信息；
- 当前表达式 transfer 暂不支持，只能保守 top；
- 编译优化改变了表达式位置，但 debug location 没有落在查询行。

排查建议：

```bash
../../build/build/bin/cjc.stage4 main.cj --dump-chir -O2 --output-type=staticlib -o debug_static
rg -n 'Debug|变量名|main.cj' debug_static_CHIR
```

### 13.4 `/usr/bin/clang++: No such file or directory`

原因：`build/build` 是旧构建缓存，里面记录了 `/usr/bin/clang++`。

解决方式优先级：

1. 清理并重新配置：

   ```bash
   python3 build.py clean
   python3 build.py build -t release -j$(nproc)
   python3 build.py install
   ```

2. 如果确实想继续用旧 build cache，就安装或提供对应路径的 `clang++`，但这不推荐作为交付方案。

### 13.5 LLVM API 版本不对

原因通常是 CMake 找到了错误的 LLVM 头文件或库。

解决：

- 不要手工指定系统 LLVM；
- 清理 build cache；
- 确保使用仓颉项目内的 LLVM；
- 重新运行 `build.py build`。

## 14. 交付建议

给别人拉取编译时，推荐说明：

```bash
cd /home/gunseller/project/cangjie_compiler
python3 build.py clean
python3 build.py build -t release -j$(nproc)
python3 build.py install
source ./output/envsetup.sh
cjc --version
```

验证 range analysis：

```bash
cd /home/gunseller/project/cangjie_compiler/range_analysis_stage4/e2e_expr
rm -rf stage4_expr_static stage4_expr_static_CHIR default.cjo output.txt
CANGJIE_HOME=/home/gunseller/project/cangjie_compiler/output \
/home/gunseller/project/cangjie_compiler/output/bin/cjc main.cj \
  -O2 \
  --output-type=staticlib \
  -o stage4_expr_static
diff -u expected_output.txt output.txt
```

如果使用当前临时阶段版：

```bash
cd /home/gunseller/project/cangjie_compiler/range_analysis_stage4/e2e_expr
export CANGJIE_HOME=/home/gunseller/project/cangjie_compiler/output
../../build/build/bin/cjc.stage4 main.cj -O2 --output-type=staticlib -o stage4_expr_static
diff -u expected_output.txt output.txt
```

总结一句：`output.txt` 不是普通编译器输出，而是当前 range analysis 比赛逻辑在 `-O2` CHIR range propagation 中读取当前目录 `input.txt` 后主动写出的结果文件。
